@model IEnumerable<BanVeMayBay.Models.ticket>
@{ var sum = 0;}
@foreach (var item in Model)
    {

        <table id="myTable1" style="margin-top:20px;">
            <tbody>
                <tr>
                    <td class="title">Giá vé</td>
                    <td class="b-id text-danger">@item.price.Value.ToString("N0") VNĐ</td>
                </tr>
                <tr>
                    <td class="title">Ngày đi</td>
                    <td>@item.departure_date.Value.ToString("dd/MM/yyyy")</td>
                </tr>
                <tr>
                    <td class="title">Nơi đi</td>
                    <td>@item.city.cityName</td>
                </tr>
                <tr>
                    <td class="title">Nơi đến</td>
                    <td>@item.city1.cityName</td>
                </tr>
                <tr>
                    <td class="title">Thời gian bay</td>
                    <td>@item.departure_time</td>
                </tr>
                <tr>
                    <td class="ArriveTime">Thời gian đến</td>
                    <td>@item.arrival_time</td>
                </tr>
            </tbody>
        </table>

        sum += (int)item.price;
    }

